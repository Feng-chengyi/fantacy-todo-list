# Third-Party Notices

本项目源代码以 MIT 许可发布（见 `LICENSE`）。

桌宠为纯 2D 自绘角色（SVG / CSS），**不依赖 Live2D 或其它第三方运行时资产**，无需要单独分发或遵守额外许可的内部资源。

其余第三方 npm 依赖（React、Electron 等）遵循其各自的开源许可，详见各包内的许可文件与 `package-lock.json`。